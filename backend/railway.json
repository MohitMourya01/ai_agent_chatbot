{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "dockerfilePath": "backend/Dockerfile"
    },
  "deploy": {
    "startCommand": "uvicorn backend.src.main:app --host 0.0.0 --port 8000"
    }
}